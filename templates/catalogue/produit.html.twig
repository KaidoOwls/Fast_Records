{% extends 'base.html.twig' %}

{% block body %}

    {# Conteneur pour le carrousel #}
    <div id="demo" class="carousel slide position-absolute top-50 start-50 translate-middle" style="z-index: 1;" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="card" style="background-color: lightyellow; color: black;">
                    <div class="row">
                        <div class="col-md-6 text-center align-self-center">
                            <img class="img-fluid" src="https://mdbcdn.b-cdn.net/img/new/standard/nature/111.webp" alt="{{ produit.getLibelle() }}">
                        </div>
                        <div class="col-md-6 info">
                            <div class="row title">
                                <div class="col"><h2>{{ produit.getLibelle() }}</h2></div>
                                <div class="col text-right"><a href="#"><i class="fa fa-heart-o"></i></a></div>
                            </div>
                            <p>{{ produit.getDescription() }}</p>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star-half-full"></span>
                            <span id="reviews">1590 Avis</span>
                            <div class="row price">
                                <label class="radio">
                                    <span>
                                        <div class="row">{{ produit.getPrix() }} €</div>
                                    </span>
                                </label>
                            </div>
                        </div>
                    </div>
                        <div class="col-12">
                            <a href="/add/{{produit.id}}" class="btn btn-primary">Ajouter au panier</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
